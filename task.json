{
  "name": "Task Manager API",
  "version": "1.0.0",
  "description": "A RESTful API for managing tasks using Node.js and Express.js",
  "endpoints": [
    {
      "method": "GET",
      "path": "/api/tasks",
      "description": "Get all tasks with optional filtering"
    },
    {
      "method": "GET",
      "path": "/api/tasks/:id",
      "description": "Get a specific task by ID"
    },
    {
      "method": "POST",
      "path": "/api/tasks",
      "description": "Create a new task"
    },
    {
      "method": "PUT",
      "path": "/api/tasks/:id",
      "description": "Update an existing task"
    },
    {
      "method": "DELETE",
      "path": "/api/tasks/:id",
      "description": "Delete a task"
    }
  ],
  "validation": [
    "Title is required for all tasks",
    "Title must be a non-empty string",
    "Title cannot exceed 200 characters",
    "Completed field must be a boolean"
  ]
}